# GitHub Pages configuration
# This file is intentionally minimal — the site is plain HTML/CSS/JS.
# To deploy: push the contents of this folder to a GitHub repository,
# then go to Settings > Pages and set Source to "main" branch, root folder.

title: George Alexander Vela
description: Author of Greek Mythology Literary Fiction
